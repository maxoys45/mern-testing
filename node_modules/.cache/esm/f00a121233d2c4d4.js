let mongoose;_33d‍.x([["Item",()=>Item]]);_33d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const Schema = mongoose.Schema

const ItemSchema = new Schema({
  name: {
    type: String,
    required: true,
  },
  date: {
    type: Date,
    default: Date.now,
  },
})

       const Item = mongoose.model('item', ItemSchema)
